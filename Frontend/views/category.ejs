<%- include('includes/head') %>
  <link rel="stylesheet" href="/styles/category.css">
  </head>

  <body>
    <%- include('includes/nav') %>

      <main>
        <section class="index-category">
          <p>หมวดหมู่</p>

          <% if (categories && categories.length> 0) { %>
            <% categories.forEach(cat=> { %>
              <a href="/category?categoryId=<%= cat.category_id %>&sizeId=<%= selectedSize %>&search=<%= searchTerm || '' %>"
                class="index-category-box <%= selectedCategory === cat.category_id ? 'active' : '' %>">
                <h3>
                  <%= cat.category_name %>
                </h3>
              </a>
              <% }) %>
                <% } else { %>
                  <p>ยังไม่มีหมวดหมู่ในระบบ</p>
                  <% } %>
        </section>

        <form action="/category" method="get" class="filter-form">
          <input type="hidden" name="categoryId" value="<%= selectedCategory %>">
          <input type="text" name="search" placeholder="ค้นหาชื่อสินค้า" value="<%= searchTerm %>">

          <div class="size-filter">
            <input type="number" name="chest" placeholder="อก (cm)" value="<%= chest || '' %>" step="0.1">
            <input type="number" name="waist" placeholder="เอว (cm)" value="<%= waist || '' %>" step="0.1">
            <input type="number" name="hips" placeholder="สะโพก (cm)" value="<%= hips || '' %>" step="0.1">
            <button type="submit">กรอง</button>
          </div>
        </form>

        <!-- 🛍 สินค้าทั้งหมด -->
        <section id="all_product" class="section-p1 product1">
          <h2>ชุดทั้งหมด</h2>
          <p>สามารถดูแต่ละหมวดหมู่ได้ที่หมวดหมู่</p>


          <div class="pro-container">
            <% if (products && products.length> 0) { %>
              <% products.slice(0,4).forEach(product=> { %>
                <%- include('includes/productCard', { product }) %>
                  <% }) %>
                    <% } else { %>
                      <p>ยังไม่มีสินค้าในระบบ</p>
                      <% } %>
          </div>
        </section>
      </main>

      <%- include('includes/footer') %>
        <%- include('includes/chatWidget') %>
          <script src="/js/script.js"></script>
          <script src="/js/heart-toggle.js"></script>
  </body>

  </html>