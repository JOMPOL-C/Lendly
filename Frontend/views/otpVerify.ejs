<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title || 'ยืนยัน OTP - Lendly' %></title>
  <link rel="stylesheet" href="/styles/style.css"/>
  <script src="/script.js" defer></script>
  <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet"/>
  <link href="https://cdn.boxicons.com/fonts/basic/boxicons.min.css" rel="stylesheet"/>
</head>
<body>
  <div class="logo">
    <img src="/images/LogoLenedly-removebg-preview.png" alt="Lendly Logo"/>
  </div>

  <div class="wrapper">
    <div class="otp-icon">
      <i class="box bxs-check-shield"></i>
    </div>

    <h1>Enter OTP</h1>

    <form id="otpForm" method="post" action="/auth/verify-otp">
      <input type="hidden" name="email" value="<%= email || '' %>" />

      <div class="input-forget">
        <input type="text" inputmode="numeric" name="d1" maxlength="1" required />
        <input type="text" inputmode="numeric" name="d2" maxlength="1" required />
        <input type="text" inputmode="numeric" name="d3" maxlength="1" required />
        <input type="text" inputmode="numeric" name="d4" maxlength="1" required />
      </div>

      <div class="input-box">
        <div class="input-field">
          <button id="forget" type="submit" class="btn">ถัดไป</button>
        </div>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <p id="error"><%= error %></p>
      <% } %>
    </form>
  </div>
</body>
</html>
