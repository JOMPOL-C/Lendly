<div class="pro">
    <a href="/api/products/<%= product.product_id %>" class="product-media">
      <% if (product.images && product.images.length > 0) { %>
        <img src="<%= product.images[0].image_url %>" alt="<%= product.product_name %>">
      <% } else { %>
        <img src="/images/no-image.png" alt="no image">
      <% } %>
    </a>
    <a href="#" class="btn-pro" aria-label="เพิ่มรายการโปรด">
      <i 
        class='bx <%= likedIds && likedIds.includes(product.product_id) ? "bxs-heart" : "bx-heart" %> btn' 
        data-id="<%= product.product_id %>" 
        style="<%= likedIds && likedIds.includes(product.product_id) ? 'color: crimson;' : '' %>">
      </i>
    </a>
  
    <div class="des">
      <h5><%= product.product_name %></h5>
      <span><%= product.category ? product.category.category_name : '' %></span>
    </div>
  
    <div class="product-card-footer">
      <% if (product.prices && product.prices.length > 0) { %>
        <div class="price-options">
          <span><small>เทส</small><strong><%= product.prices[0].price_test %>฿</strong></span>
          <span><small>ไพร</small><strong><%= product.prices[0].price_pri %>฿</strong></span>
        </div>
      <% } else { %>
        <h4>ไม่มีราคา</h4>
      <% } %>
    </div>
  </div>
  
